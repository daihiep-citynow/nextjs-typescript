import type { NextPage } from "next";

import styles from "./Button.module.scss";

const Button: NextPage = () => (
  <div className={styles["button-wrapper"]}>success 1</div>
);

export default Button;
